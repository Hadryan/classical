%h1
  = h @instrument.name
= link_to 'Edit', edit_instrument_path(@instrument)
|
= link_to 'Back', instruments_path

.albums
  .title Albums:

  = render :partial => 'albums/album_headers'
  .list= render :partial => @instrument.albums

