<div class="classical_form">
  <fieldset class="field_form">
    <legend>Instument Details</legend>

    <% form_for( instrument ) do |f| %>
      <%= f.error_messages %>

      <p>
        <%= f.label :description %>
        <%= f.text_field :name %>
      </p>
      <p>
        <%= f.submit button_label %>
      </p>
    <% end %>
  </fieldset>
</div>

<%= link_to 'Back', instruments_path %>