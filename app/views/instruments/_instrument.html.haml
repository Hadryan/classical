#row
  #data
    = link_to instrument.name, instrument, :id => 'link_to_data'
  #controls
    = link_to "Edit", edit_instrument_path(instrument)
    = link_to "#{t :delete}", instrument, :confirm => 'Are you sure?', :method => :delete
