.classical_form
  %h2
    = "#{t :obratype}"
  - form_for( obraType ) do |f|
    = f.error_messages
    .form_element
      = f.label :description
      = f.text_field :name, :class => :text
    %p
      = f.submit button_label

= link_to 'Back', obra_types_path

