= title t :obratypes

%h1
  = t :obratypes

= link_to "#{t :new}", :new_obra_type

- form_tag do
  %p
    = t :search
    = text_field_tag :search_field
    = image_tag 'spinner.gif', :id => 'spinner', :style => 'display: none'

#results
  = render :partial => @obra_types

= will_paginate @obra_types

= link_to "Home", :controller => :home
= observe_field 'search_field', :url => {:action => 'obra_completion'}, :frequency => 1, :before => "Element.show('spinner')", :complete => [ visual_effect(:highlight, :results), "Element.hide('spinner')"], :update => :results, :with => 'prefix'

