<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
    <title>Classical - Composers</title>
  </head>
  <body>
    <h1>Composers</h1>
    <%= link_to "New", :new_composer %>
    <br>
    <% @composers.each do | composer | %>
      <p>
        <%=h composer.name %>
		<%h (' (' + composer.birthDate.year.to_s + '-' + composer.deathDate.year.to_s + ')') if !composer.birthDate.nil? %>
        
        <%= link_to "Show", composer %>
        <%= link_to "Edit", edit_composer_path(composer) %>
        <%= link_to "Delete", composer, :confirm => 'Are you sure?', :method => :delete %>
      </p>
    <% end %>
    <br>
    <%= link_to "Home", :controller => :home %>
  </body>
</html>