<div class="classical_form">
  <fieldset class="field_form">
    <legend>Orchestra Details</legend>
    
    <% form_for( orchestra ) do |f| %>
      <%= f.error_messages %>

      <p>
        <%= f.label :description %><br />
        <%= f.text_field :name %>
      </p>
      <p>
        <%= f.submit button_label %>
      </p>
    <% end %>
  </fieldset>
</div>

<%= link_to 'Back', orchestras_path %>