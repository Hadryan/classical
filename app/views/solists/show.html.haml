%h1
  = h @solist.name
= link_to 'Edit', edit_solist_path(@solist)
|
= link_to 'Back', solists_path

.albums
  .title Albums:

  = render :partial => 'albums/album_headers'
  .list= render :partial => @solist.albums

