%tr
  %td= link_to director.name, director, :id => 'link_to_data'
  %td= link_to "Edit", edit_director_path(director)
  %td= link_to "#{t :delete}", director, :confirm => 'Are you sure?', :method => :delete

